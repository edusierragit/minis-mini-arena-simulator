# Public GoatCounter endpoint; this is not a secret.
VITE_GOATCOUNTER_ENDPOINT=https://YOUR-ACCOUNT.goatcounter.com/count

# Use this instead on Cloudflare Pages for same-origin, first-party collection.
# VITE_FIRST_PARTY_ANALYTICS_ENDPOINT=/api/analytics

# Enable only on the old host after the canonical deployment is verified.
# VITE_CANONICAL_DEPLOYMENT_URL=https://YOUR-PROJECT.pages.dev/
